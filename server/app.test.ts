import { afterEach,describe,expect,it } from "vitest"; import { buildApp } from "./app.js";
const apps:ReturnType<typeof buildApp>[]=[]; afterEach(async()=>Promise.all(apps.splice(0).map(app=>app.close())));
describe("health endpoints",()=>{it("reports liveness without database access",async()=>{const app=buildApp(async()=>{throw new Error("must not run")});apps.push(app);const response=await app.inject({method:"GET",url:"/health/live"});expect(response.statusCode).toBe(200);expect(response.json()).toEqual({status:"ok"});});it("reports readiness when database is available",async()=>{const app=buildApp(async()=>undefined);apps.push(app);const response=await app.inject({method:"GET",url:"/health/ready"});expect(response.statusCode).toBe(200);});it("fails readiness when database is unavailable",async()=>{const app=buildApp(async()=>{throw new Error("offline")});apps.push(app);const response=await app.inject({method:"GET",url:"/health/ready"});expect(response.statusCode).toBe(503);});});
